[project]
name = "guardrails-ai-simlab-client"
version = "0.1.0"
authors = ["Guardrails AI <contact@guardrailsai.com>"]
license = "MIT"

description = "client wrapper for usage with llm experiment"
readme = "README.md"
requires-python = ">= 3.9"
dependencies = [
    "requests = ">=2.31.0"
]
